version: "3"
services:
  mysqldb:
    image: mysql:latest
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=achatdb
      - MYSQL_PASSWORD=root
    volumes:
      -  /home/mysql/data:/var/lib/mysql
    networks:
      - ci-net  

  spring-service:
    image: ibrahimbouafif99/achat
    ports:
      - 8089:8089
    environment :
      -SPRING_DATASOURCE_URL: jdbc:mysql://mysqldb/achatdb
      -SPRING_DATASOURCE_USERNAME : root
      -SPRING_DATASOURCE_PASSWORD : root
    depends_on:
      - mysqldb
    networks:
      - ci-net
networks:
  ci-net:
